// Powered by OnSpace.AI
// This file is intentionally minimal — routing is handled by app/index.tsx
import { Redirect } from 'expo-router';

export default function TabsIndex() {
  return <Redirect href="/" />;
}
